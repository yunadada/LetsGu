export type LoginInput = {
  email: string;
  password: string;
};

export type UserProfileData = {
  email: string;
  imageUrl: string;
  nickname: string;
};

export type UserInfo = {
  memberId: number;
  imageUrl: string;
};
